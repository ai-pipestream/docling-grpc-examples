[project]
name = "docling-grpc-python-example"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "grpcio>=1.71.0",
  "grpcio-tools>=1.71.0",
  "protobuf>=5.29.0"
]

[tool.uv]
package = false
